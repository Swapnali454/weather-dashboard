web: gunicorn weather_project.wsgi --log-file -
```

**What this means:**
- `web:` - This is a web application
- `gunicorn` - Use Gunicorn server to run Django (production server)
- `weather_project.wsgi` - Start the Django project
- `--log-file -` - Show logs in the console

---

### **Step 4: Save the File**

Press **Ctrl + S** to save.

---

## **Step-by-Step: Create runtime.txt**

### **Step 1: Create Another New File**

1. Right-click in the sidebar again
2. Click **"New File"**
3. Type: `runtime.txt`
4. Press **Enter**

---

### **Step 2: Add Content to runtime.txt**

Click on `runtime.txt` and paste this:
```
python-3.11.0